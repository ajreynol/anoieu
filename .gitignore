__pycache__/
*.pyc
.venv/

# the sources a report is generated from: a cache, rebuilt by tools/deps.py
deps/

# what a fuzzing run finds: reproducers, kept until somebody triages them
fuzz-findings/

# where each project is checked out on this machine, written by
# scripts/process_anoieu on first run
scripts/repos.local

# editor scratch
*.swp
*.swo

# transient working space (tools/policy.md, "Working space is untracked")
scratch/

# private working notes, untracked by intent
*.local.md
