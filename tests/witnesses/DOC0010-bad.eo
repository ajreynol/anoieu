; expect: DOC0010
(declare-const Int Type)
(declare-parameterized-const = ((T Type :implicit)) (-> T T Bool))

; rule: symmetry
; conclusion: The equality, flipped.
(declare-rule symm ((T Type) (t T) (s T))
  :premises ((= t s))
  :conclusion (= s t)
)
