# The commit of koine our bug database is appended to by.
#
# A pin is a choice, not a fact: moving it changes what maintains our record.
# The ecosystem's rule is that a pin only moves to a commit where the other
# project's CI is green.
#
# 8efe59c -- use bug_db_manager/koine_append_db; the old bug_db/ directory
# has been removed. Arguments and JSON format are unchanged. Verified against
# remote main on 2026-09-18; the koine suite and anoieu policy job both completed
# successfully for this exact commit:
# https://github.com/ajreynol/koine/actions/runs/35383263886
# https://github.com/ajreynol/koine/actions/runs/35383264452
#
# 567c4a1 -- koine_append_db moved to bug_db/koine_append_db, when koine gave
# each of its two purposes (bug databases, and the eo command) a directory of
# its own. The script itself is byte-identical to the one at the earlier pin,
# fc31e8d, so this moved a path and nothing else; the adapter moved with it.
# koine's CI at this commit is test_append_db.py and test_install_eo_cmd.py,
# and both passed.
#
# fc31e8d -- koine_append_db, the whole of koine, at the root of a checkout.
# The pin before that, d445add, was a reporting-loop library with a findings
# record in it; that was deleted on 2026-09-16 when koine was pointed at this
# one job, and we moved with it.
8efe59ca20b5d684d8d00fce330b6a6968444493
